---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ProjectCard from '../components/ProjectCard.astro';
import projectsData from '../data/projects.json';
import { buildBreadcrumbSchema } from '../lib/seo';
const breadcrumbs = buildBreadcrumbSchema([
  { name: 'Accueil', path: '/' },
  { name: 'Réalisations' }
]);
---

<Layout title="Réalisations Demo Solution | Avant-après en décontamination" description="Réalisations Demo Solution en décontamination, vermiculite, échantillonnage et démolition sélective à Gatineau, Ottawa et Québec." canonicalPath="/realisations/" frPath="/realisations/" enPath="/en/realisations/" schemas={[breadcrumbs]}>
  <Header />

  <main>
    <section class="page-header">
      <div class="container">
        <p class="eyebrow">Interventions complètes</p>
        <h1>Nos Réalisations</h1>
        <p>Exemples d’interventions réelles en décontamination et travaux préparatoires</p>
      </div>
    </section>

    <section class="realisations-section">
      <div class="container">
        <div class="section-intro">
          <h2>Avant-après et interventions réelles</h2>
          <p>Ces réalisations montrent des exemples de travaux effectués sur des bâtiments résidentiels et commerciaux: décontamination, moisissures, enlèvement de vermiculite, échantillonnage et démolition sélective. Cette section renforce la preuve de travail et aide à situer le type d’intervention possible selon votre dossier.</p>
        </div>
        <div class="projects-grid">
          {projectsData.items.map((project) => (
            <ProjectCard
              id={project.id}
              title={project.title}
              location={project.location}
              category={project.category}
              beforeImage={project.beforeImage}
              afterImage={project.afterImage}
              beforeAlt={project.beforeAlt}
              afterAlt={project.afterAlt}
              description={project.description}
            />
          ))}
        </div>
      </div>
    </section>

    <section class="seo-copy">
      <div class="container seo-columns">
        <div>
          <h2>Pourquoi ces projets comptent</h2>
          <p>Un avant-après aide à comprendre le type de problème traité, la nature des matériaux touchés et le niveau d’intervention requis selon le bâtiment. C’est aussi une bonne façon de voir la différence entre échantillonnage, décontamination et démolition préparatoire.</p>
        </div>
        <div>
          <h2>Travaux fréquents en atelier</h2>
          <p>Parmi les demandes les plus courantes, on retrouve l’amiante, la vermiculite, les moisissures, les matériaux suspects et les sections à retirer proprement avant la suite des travaux. Si votre situation ressemble à l’un de ces cas, nous pouvons vous orienter vers le bon service.</p>
        </div>
      </div>
    </section>

  </main>

  <Footer />
</Layout>

<style>
  .page-header {
    background:
      radial-gradient(circle at top left, rgba(242, 210, 27, 0.18), transparent 25%),
      linear-gradient(135deg, var(--brand-navy-dark) 0%, #1f476d 58%, #eef3f7 58%, #eef3f7 100%);
    color: white;
    padding: 4.5rem 0 3.8rem;
  }

  .eyebrow {
    display: inline-flex;
    margin-bottom: 1rem;
    padding: 0.45rem 0.85rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.12);
    color: var(--brand-gold);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .page-header h1 {
    color: white;
    margin-bottom: 0.75rem;
    font-size: 3rem;
  }

  .page-header p:last-child {
    margin: 0;
    max-width: 55ch;
    font-size: 1.08rem;
    color: #e7edf4;
  }

  .realisations-section {
    padding: 4rem 0;
    background: white;
  }

  .section-intro {
    max-width: 850px;
    margin-bottom: 2rem;
  }

  .section-intro p {
    margin-top: 0.75rem;
    color: var(--brand-muted);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .seo-copy {
    padding: 0 0 4rem;
    background: white;
  }

  .seo-columns {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .seo-columns p {
    margin-top: 0.75rem;
    color: var(--brand-muted);
  }

  @media (max-width: 768px) {
    .page-header {
      background: linear-gradient(180deg, var(--brand-navy-dark) 0%, #1f476d 100%);
      padding: 3.6rem 1.5rem 3rem;
    }

    .page-header h1 {
      font-size: 2rem;
    }

    .page-header p {
      font-size: 1rem;
    }

    .realisations-section {
      padding: 2rem 0;
    }

    .projects-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
